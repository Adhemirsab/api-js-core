{
  "name": "api-js-core",
  "version": "1.0.0",
  "description": "core",
  "type": "module",
  "scripts": {
    "esbuild:ts": "esbuild src/functions/*.ts --bundle --platform=node --outdir=dist --format=esm",
    "esbuild": "esbuild src/functions/*.js --bundle --platform=node --outdir=dist --format=esm",
    "build": "npm run esbuild && npm run esbuild:ts",
    "test": "vitest"
  },
  "license": "ISC",
  "devDependencies": {
    "esbuild": "^0.19.3",
    "typescript": "^5.2.2"
  }
}
