{
  "name": "api-js-core",
  "version": "1.0.0",
  "description": "core",
  "type": "module",
  "scripts": {
    "build": "esbuild src/functions/*.ts --bundle --minify --platform=node --outdir=dist --format=esm",
    "test": "vitest"
  },
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^20.6.5",
    "esbuild": "^0.19.3"
  }
}
