{
  "name": "api-js-core",
  "version": "1.0.0",
  "description": "core",
  "type": "module",
  "scripts": {
    "tsc": "tsc",
    "esbuild": "esbuild src/functions/*.js --bundle --platform=node --outdir=dist --format=esm",
    "build": "npm run tsc && npm run esbuild",
    "test": "vitest"
  },
  "license": "ISC",
  "devDependencies": {
    "esbuild": "^0.19.3",
    "typescript": "^5.2.2"
  }
}
