{
  "name": "api-js-core",
  "version": "1.0.0",
  "description": "core",
  "scripts": {
    "build": "esbuild src/functions/*.ts --bundle --minify --platform=node --outdir=dist --format=cjs",
    "prettier:check": "prettier . --check",
    "prettier:write": "prettier . --write",
    "test": "vitest"
  },
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^20.6.5",
    "esbuild": "^0.19.3",
    "prettier": "^3.0.3"
  },
  "dependencies": {
    "axios": "^1.5.0"
  }
}
